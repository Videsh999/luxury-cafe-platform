{
  "name": "luxury-cafe-platform",
  "version": "1.0.0",
  "description": "Root package for deployment",
  "scripts": {
    "install": "npm install --prefix frontend && npm install --prefix backend && npm install --prefix admin",
    "build": "npm run build --prefix frontend && npm run build --prefix admin",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
